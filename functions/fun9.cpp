#include <iostream>
#include <cmath>
using namespace std;

int main()
{
	int i = -5;
	double x = 5.0, y = 2.0, z = 5.21;

	cout<<"the absolute value of i is "<<abs(i)<<"\n";
	cout<<"the power of x to the power y is "<<pow(x, y)<<"\n";
	cout<<"the floor for z is "<<floor(z)<<"\n";
	cout<<"the ceil z is "<<ceil(z)<<"\n";
}